<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sudhakar.vivaregistration">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/vivavvit"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".EventRegistrationActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".GroupRegistrationActivity" />
        <activity android:name=".GrroupActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".JudgeActivity" />
        <activity android:name=".WinnerSelectionActivity" />
        <activity android:name=".WinnerActivity" />
        <activity android:name=".GroupWinnerActivity"></activity>
    </application>

</manifest>